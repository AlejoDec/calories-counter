
export interface FoodCalorieInfo {
  foodItem: string;
  caloriesPerItem: number; // Calories for a single item
  quantity: number;        // Number of identical items
  servingSize: string;     // Serving size for a single item
}
